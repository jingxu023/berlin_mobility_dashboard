# Processed data

Analysis-ready outputs generated from raw data belong here.

Planned outputs:
- `cycling_hourly.parquet`
- `cycling_daily.parquet`
- `roadworks.parquet` or GeoParquet
- `berlin_bezirke.geojson`

These outputs should be reproducible from scripts in `src/`.
